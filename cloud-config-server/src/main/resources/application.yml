server:
  port: 9296

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mostafa-semnani-74/config-server.git
          clone-on-start: true
